export interface Sample {
  string: string;
  number: number;
  boolean: boolean;
  object: object;
}

export interface loginInfo {
  email: string;
  password: string;
}

export interface signInfo {
  realName: string;
  displayName: string;
  email: string;
  password: string;
  phone: string;
  birthDate: string;
  mailKey: string;
}
