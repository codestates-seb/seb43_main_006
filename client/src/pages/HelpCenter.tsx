import React from "react";
import styled from "styled-components";

const HelpCenterContainer = styled.section``;

const HelpCenter = () => {
  return <HelpCenterContainer>Payment!!</HelpCenterContainer>;
};

export default HelpCenter;
